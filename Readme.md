# Usage

change `_shibsession_***...` and `PHPSESSID` in main.py (can be found with chrome devtools after login to ilias)  
Console Application that prompts url; scrapes page for all video links & downloads videos to given target_dir